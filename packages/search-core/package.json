{
  "name": "@pictelio/search-core",
  "version": "0.1.0",
  "description": "Pictelio 搜索共享核心（筛选状态 → 请求参数 / URL 编解码 / 缓存键纯函数），webview 与 app-lynx 单点共用（spec docs/specs/search-advanced-filters.md）",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^4.1.10"
  }
}
