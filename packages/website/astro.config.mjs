import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  site: "https://a1121611810.github.io",
  base: "/pixivizer",
  trailingSlash: "always",
});
