{
  "name": "@pictelio/net-diagnostics",
  "version": "0.1.0",
  "description": "Pictelio 网络自检共享纯逻辑库（检查项计划 / 判定 / 文案 / 报告），webview 与 lynx 共用",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "fast-check": "^4.9.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.10"
  }
}
