<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <!-- T3（解 ADR-0168 D4 钉死，spec lynx-night-mode §splash 双轨 · 深色 plate）：
             API 31+（Android 12+）系统 splash 在 launcher icon 非全屏时使用本色作为 plate；
             亮色模式下与背景同色（视觉一致性）。暗色覆写见 values-night/styles.xml。 -->
        <item name="windowSplashScreenIconBackgroundColor">@android:color/white</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>

    <!-- T3（spec lynx-night-mode §splash 双轨 · 兜底轨亮色变体）：API 31+ 平台 setSplashScreenTheme 显式覆盖主题。
         only-defined-in-values/，运行时被初值 uiMode=light 选中。暗色覆写见 values-night/styles.xml。 -->
    <style name="Theme.SplashScreen.Light" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">@android:color/white</item>
    </style>
</resources>