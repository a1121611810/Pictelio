{
  "name": "@pictelio/ranking-core",
  "version": "0.1.0",
  "description": "Pictelio 排行榜共享核心（维度目录 / 请求构建 / 缓存键 / 日期纯函数），webview 与 app-lynx 单点共用（spec docs/specs/ranking.md）",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^4.1.10"
  }
}
